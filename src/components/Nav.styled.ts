import {styled} from 'styled-components'

export const StyledNav = styled.nav`
  display: flex;
  height: 200px;
  align-items: center;
  justify-content: space-between;

  svg {
    flex: 0 0 150px;
  }

  #packs {
    display: flex;
    gap: 2rem;

    img {
    }
  }
`
